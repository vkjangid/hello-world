pip install module_name

#it will download the module

pip uninstall module_name

#it will uninstall the module

pip install -U module_name

#it will upgrade the package

pip list -o

#it will show the modules which are not updated
