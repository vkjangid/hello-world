# abstaction

from oops14 import *

# it will work when oops14 will be present in the same folder

class Generate_random_number(RandNumber):
    print(RandNumber.get_random_no(40))


# we can import a file from another directory by using sys module
''' import sys
    sys.'file_path' '''